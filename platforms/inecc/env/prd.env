# Prod environment overrides for inecc
# These values are passed to create-envfile.py on top of the platform base config
hostname=inecc.geoint.mx
env_type=prod
oidc_provider_url=https://inecc.geoint.mx/iam/realms/inecc
https_mode=externalhttps
