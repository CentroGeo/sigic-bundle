# QA environment overrides for idce
# These values are passed to create-envfile.py on top of the platform base config
hostname=idce.qa.geoint.mx
env_type=test
oidc_provider_url=https://idce.qa.geoint.mx/iam/realms/idce
https_mode=https
