# Prod environment overrides for idce
# These values are passed to create-envfile.py on top of the platform base config
hostname=idce.prod.geoint.mx
env_type=prod
oidc_provider_url=https://idce.prod.geoint.mx/iam/realms/idce
https_mode=externalhttps
